[package]
name = "eidos-protocol"
description = "Shared types and contracts for Eidos (WM, chat, tools, agents config)"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
